import * as vscode from 'vscode';
import { expect } from 'chai';


describe('index', () => {
    it('test work', async function () {
        // const [result] = await Promise.all([buildResponse(payload)]);
        expect(1).equal(1);
    });
});